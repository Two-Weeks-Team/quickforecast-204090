"use client";

{
  "import": "use client;\nimport { useEffect, useRef } from "react";",
  "export": "const WaterfallChart = ({ data }) => {\n  const chartRef = useRef(null);\n\n  useEffect(() => {\n    if (!chartRef.current || !data) return;\n\n    // Simple SVG-based waterfall chart\n    const svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg');\n    svg.setAttribute('width', '100%');\n    svg.setAttribute('height', '300');\n\n    const width = 800;\n    const height = 300;\n    const barWidth = 10;\n    const spacing = 20;\n    const startX = 50;\n    const startY = height - 50;\n\n    let currentBalance = 0;\n\n    data.forEach((point, index) => {\n      const barHeight = Math.abs(point.balance);\n      const y = startY - barHeight;\n      const isPositive = point.balance > 0;\n\n      const rect = document.createElementNS('http://www.w3.org/2000/svg', 'rect');\n      rect.setAttribute('x', startX + index * (barWidth + spacing));\n      rect.setAttribute('y', y);\n      rect.setAttribute('width', barWidth);\n      rect.setAttribute('height', barHeight);\n      rect.setAttribute('fill', isPositive ? '#4CAF50' : '#F44336');\n\n      const text = document.createElementNS('http://www.w3.org/2000/svg', 'text');\n      text.setAttribute('x', startX + index * (barWidth + spacing) + barWidth / 2);\n      text.setAttribute('y', startY + 20);\n      text.setAttribute('text-anchor', 'middle');\n      text.textContent = `$${point.balance.toFixed(2)}`;\n\n      svg.appendChild(rect);\n      svg.appendChild(text);\n    });\n\n    chartRef.current.innerHTML = '';\n    chartRef.current.appendChild(svg);\n  }, [data]);\n\n  return (\n    <div className=\"waterfall\" ref={chartRef}>\n      {data ? null : <p>No data to display</p>}\n    </div>\n  );\n};\n\nexport default WaterfallChart;"
}