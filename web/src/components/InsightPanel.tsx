"use client";

{
  "import": "use client;\nimport { useEffect, useState } from "react";\nimport { getForecast } from '@/lib/api';",
  "export": "const InsightPanel = () => {\n  const [forecastData, setForecastData] = useState<any>(null);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    const fetchForecast = async () => {\n      try {\n        const data = await getForecast(new Date().toISOString().split('T')[0], 30);\n        setForecastData(data);\n      } catch (error) {\n        console.error('Error fetching forecast data:', error);\n      } finally {\n        setLoading(false);\n      }\n    };\n\n    fetchForecast();\n  }, []);\n\n  if (loading) {\n    return (\n      <div className=\"insight p-4\">\n        <h2 className=\"text-xl font-bold mb-4\">Forecast Insights</h2>\n        <div className=\"animate-pulse\">\n          <div className=\"h-4 bg-white bg-opacity-50 rounded w-1/2 mb-2\"></div>\n          <div className=\"h-4 bg-white bg-opacity-50 rounded w-3/4\"></div>\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"insight p-4\">\n      <h2 className=\"text-xl font-bold mb-4\">Forecast Insights</h2>\n      <div className=\"space-y-2\">\n        <p><strong>Confidence Interval:</strong> ${forecastData?.confidence_interval.min.toFixed(2)} - ${forecastData?.confidence_interval.max.toFixed(2)}</p>\n        <p><strong>Forecast Start:</strong> {forecastData?.forecast[0]?.date}</p>\n        <p><strong>Forecast End:</strong> {forecastData?.forecast[forecastData.forecast.length - 1]?.date}</p>\n        <p><strong>Initial Balance:</strong> ${forecastData?.forecast[0]?.balance.toFixed(2)}</p>\n        <p><strong>Final Balance:</strong> ${forecastData?.forecast[forecastData.forecast.length - 1]?.balance.toFixed(2)}</p>\n      </div>\n    </div>\n  );\n};\n\nexport default InsightPanel;"
}