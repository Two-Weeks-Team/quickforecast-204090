"use client";

{
  "import": "use client;\nimport { useEffect, useState } from "react";\nimport { getDashboard } from '@/lib/api';",
  "export": "const Hero = () => {\n  const [dashboardData, setDashboardData] = useState<any>(null);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    const fetchDashboardData = async () => {\n      try {\n        const data = await getDashboard();\n        setDashboardData(data);\n      } catch (error) {\n        console.error('Error fetching dashboard data:', error);\n      } finally {\n        setLoading(false);\n      }\n    };\n\n    fetchDashboardData();\n  }, []);\n\n  if (loading) {\n    return (\n      <div className=\"hero\">\n        <h1 className=\"text-3xl font-bold mb-4\">QuickForecast</h1>\n        <p className=\"mb-4\">Instant budgeting starts with a single transaction</p>\n        <div className=\"animate-pulse\">\n          <div className=\"h-6 bg-white bg-opacity-50 rounded w-3/4 mb-2\"></div>\n          <div className=\"h-4 bg-white bg-opacity-50 rounded w-1/2\"></div>\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"hero\">\n      <h1 className=\"text-3xl font-bold mb-4\">QuickForecast</h1>\n      <p className=\"mb-4\">Instant budgeting starts with a single transaction</p>\n      <div className=\"space-y-2\">\n        <p><strong>Net Balance:</strong> ${dashboardData?.net_balance.toFixed(2)}</p>\n        <p><strong>Goals:</strong> {dashboardData?.total_goals}</p>\n        <p><strong>Upcoming Deadlines:</strong> {dashboardData?.upcoming_deadlines}</p>\n      </div>\n    </div>\n  );\n};\n\nexport default Hero;"
}